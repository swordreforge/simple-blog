# 文章历史版本管理 - 第一步检查报告

**检查时间**：2026-03-25
**检查范围**：后端基础集成（第一步）

---

## 📊 总体状态

| 任务项 | 状态 | 进度 |
|--------|------|------|
| 创建 Handler 层 | ❌ 未开始 | 0% |
| 配置 API 路由 | ❌ 未开始 | 0% |
| 集成自动保存 | ❌ 未开始 | 0% |
| 修复死代码警告 | ❌ 未开始 | 0% |

**总体进度**：0%

---

## 1. 创建 Handler 层

### 1.1 文件存在性检查

**检查项**：`src/handlers/api_handlers/passage/version.rs` 文件是否存在

**结果**：❌ **不存在**

**当前文件列表**：
```
src/handlers/api_handlers/passage/
├── crud_helper.rs
├── crud.rs
├── markdown.rs
├── mod.rs
├── query_handlers.rs
└── validation.rs
```

### 1.2 模块声明检查

**检查项**：`mod.rs` 中是否声明了 version 模块

**结果**：❌ **未声明**

**当前 mod.rs 内容**：
```rust
pub mod crud;
pub mod crud_helper;
pub mod markdown;
pub mod query_handlers;
pub mod validation;

// 缺少: pub mod version;
```

### 1.3 需要实现的 API Handler

根据集成计划书，需要实现以下 8 个 Handler：

| 函数名 | 方法 | 路径 | 状态 |
|--------|------|------|------|
| `list_versions` | GET | `/api/admin/passages/{uuid}/versions` | ❌ 未实现 |
| `create_version` | POST | `/api/admin/passages/{uuid}/versions` | ❌ 未实现 |
| `get_version` | GET | `/api/admin/passages/{uuid}/versions/{version_number}` | ❌ 未实现 |
| `delete_version` | DELETE | `/api/admin/passages/{uuid}/versions/{version_number}` | ❌ 未实现 |
| `diff_versions` | GET | `/api/admin/passages/{uuid}/versions/diff` | ❌ 未实现 |
| `restore_version` | POST | `/api/admin/passages/{uuid}/versions/{version_number}/restore` | ❌ 未实现 |
| `undo` | POST | `/api/admin/passages/{uuid}/versions/undo` | ❌ 未实现 |
| `redo` | POST | `/api/admin/passages/{uuid}/versions/redo` | ❌ 未实现 |

**实现进度**：0/8 (0%)

---

## 2. 配置 API 路由

### 2.1 路由配置检查

**检查项**：`src/routes/api_routes.rs` 中是否有版本管理路由

**结果**：❌ **未配置**

**搜索结果**：未找到任何 `passage.*version` 或 `version.*passage` 相关的路由配置

### 2.2 需要添加的路由

根据集成计划书，需要添加以下路由配置：

```rust
// 文章版本历史管理路由
web::scope("/api/admin/passages/{uuid}")
    .service(
        web::resource("/versions")
            .route(web::get().to(api_handlers::passage::version::list_versions))
            .route(web::post().to(api_handlers::passage::version::create_version))
    )
    .service(
        web::resource("/versions/{version_number}")
            .route(web::get().to(api_handlers::passage::version::get_version))
            .route(web::delete().to(api_handlers::passage::version::delete_version))
    )
    .service(
        web::resource("/versions/diff")
            .route(web::get().to(api_handlers::passage::version::diff_versions))
    )
    .service(
        web::resource("/versions/{version_number}/restore")
            .route(web::post().to(api_handlers::passage::version::restore_version))
    )
    .service(
        web::resource("/versions/undo")
            .route(web::post().to(api_handlers::passage::version::undo))
    )
    .service(
        web::resource("/versions/redo")
            .route(web::post().to(api_handlers::passage::version::redo))
    );
```

**配置进度**：0/6 (0%)

---

## 3. 集成自动保存

### 3.1 自动保存集成检查

**检查项**：`src/handlers/api_handlers/passage/crud.rs` 中是否集成了自动保存

**结果**：❌ **未集成**

**搜索结果**：
- 未找到 `auto_save_version` 调用
- 未找到 `PassageVersionService` 引用

### 3.2 需要修改的位置

**文件**：`src/handlers/api_handlers/passage/crud.rs`

**函数**：`update_passage()` 或 `update()` 函数

**集成步骤**：
1. 在文章更新成功后，检查配置是否启用历史版本
2. 如果启用，调用 `PassageVersionService::auto_save_version()`
3. 捕获并记录保存失败的情况（不影响文章更新）

**示例代码**：

```rust
// 更新文章后
if let Ok(version_service) = version_service.clone() {
    if let Err(e) = version_service.auto_save_version(
        passage.id,
        passage.uuid.clone(),
        &old_passage,
        &new_passage,
        change_reason
    ).await {
        log::warn!("自动保存文章历史版本失败: {}", e);
    }
}
```

**集成进度**：0% (尚未开始)

---

## 4. 修复死代码警告

### 4.1 死代码统计

**检查范围**：`src/` 目录下所有 `.rs` 文件

**总死代码数量**：**211 处**

### 4.2 按文件统计

| 文件 | 死代码数量 | 相关性 |
|------|-----------|--------|
| `services/passage_version_service.rs` | **35 处** | 🔴 高相关 |
| `db/models.rs` | **14 处** | 🔴 高相关 |
| `db/repositories.rs` | **2 处** | 🔴 高相关 |
| `cache/keys.rs` | **19 处** | 🟡 中相关 |
| `cache/utils.rs` | **11 处** | 🟡 中相关 |
| `services/route_storage.rs` | **13 处** | 🟡 中相关 |
| `services/route_type_manager.rs` | **12 处** | 🟡 中相关 |
| `cache/concurrent.rs` | **10 处** | 🟡 中相关 |
| `utils/unsafe_utils.rs` | **10 处** | 🟢 低相关 |
| 其他文件 | **95 处** | 🟢 低相关 |

### 4.3 高相关文件分析

#### `services/passage_version_service.rs` (35 处)

**原因**：Service 层的所有方法都已实现，但 Handler 层未实现，导致这些方法未被调用。

**示例**：
```rust
#[allow(dead_code)]
pub async fn save_version(...) { ... }

#[allow(dead_code)]
pub async fn auto_save_version(...) { ... }

#[allow(dead_code)]
pub async fn get_versions(...) { ... }
```

**解决方案**：创建 Handler 层并调用这些方法后，可以移除 `#[allow(dead_code)]`。

#### `db/models.rs` (14 处)

**原因**：数据模型定义了但未被使用。

**示例**：
```rust
#[allow(dead_code)]
pub struct PassageVersion { ... }

#[allow(dead_code)]
pub enum RestoreMode { ... }

#[allow(dead_code)]
pub struct PassageHistorySettings { ... }
```

**解决方案**：这些模型在 Service 层被使用，但 Service 层本身被标记为死代码。创建 Handler 层后可解决。

#### `db/repositories.rs` (2 处)

**原因**：Repository 方法未被调用。

**示例**：
```rust
#[allow(dead_code)]
impl PassageVersionRepository { ... }
```

**解决方案**：同上，创建 Handler 层后可解决。

### 4.4 修复计划

**优先级**：
1. 🔴 **高优先级**：实现 Handler 层后，移除 3 个高相关文件的 `#[allow(dead_code)]`（共 51 处）
2. 🟡 **中优先级**：根据实际使用情况，移除缓存相关文件的死代码警告（共 65 处）
3. 🟢 **低优先级**：其他文件的死代码警告需要逐个分析是否真的未被使用（共 95 处）

**预计移除数量**：**51-116 处**

**修复进度**：0% (尚未开始)

---

## 📋 待办事项清单

### 立即开始（高优先级）

- [ ] **创建 Handler 文件**
  - [ ] 创建 `src/handlers/api_handlers/passage/version.rs`
  - [ ] 在 `mod.rs` 中声明 version 模块
  - [ ] 导出 Handler 函数

- [ ] **实现 API Handler**
  - [ ] 实现 `list_versions` Handler
  - [ ] 实现 `create_version` Handler
  - [ ] 实现 `get_version` Handler
  - [ ] 实现 `delete_version` Handler
  - [ ] 实现 `diff_versions` Handler
  - [ ] 实现 `restore_version` Handler
  - [ ] 实现 `undo` Handler
  - [ ] 实现 `redo` Handler
  - [ ] 添加权限验证和错误处理

- [ ] **配置 API 路由**
  - [ ] 在 `src/routes/api_routes.rs` 中添加版本管理路由
  - [ ] 测试路由配置

- [ ] **集成自动保存**
  - [ ] 在 `src/handlers/api_handlers/passage/crud.rs` 中集成自动保存
  - [ ] 测试自动保存功能

- [ ] **修复死代码警告**
  - [ ] 移除 `src/services/passage_version_service.rs` 中的 `#[allow(dead_code)]` (35 处)
  - [ ] 移除 `src/db/models.rs` 中的 `#[allow(dead_code)]` (14 处)
  - [ ] 移除 `src/db/repositories.rs` 中的 `#[allow(dead_code)]` (2 处)

### 后续优化（中优先级）

- [ ] 分析缓存相关文件的死代码情况
- [ ] 根据实际使用情况移除不必要的死代码警告
- [ ] 完善单元测试和集成测试

---

## 🎯 下一步行动

**建议**：立即开始第一步实施，优先完成以下任务：

1. **创建 `version.rs` 文件**（预计 1 小时）
2. **实现 8 个 API Handler**（预计 2-3 小时）
3. **配置 API 路由**（预计 30 分钟）
4. **集成自动保存**（预计 30 分钟）
5. **测试 API 端点**（预计 1 小时）
6. **修复死代码警告**（预计 30 分钟）

**总预计时间**：5-7 小时

---

## 📊 进度跟踪

| 日期 | 任务 | 状态 | 备注 |
|------|------|------|------|
| 2026-03-25 | 第一步检查 | ✅ 完成 | 检查报告已生成 |
| - | 创建 Handler 层 | ⏳ 待开始 | - |
| - | 配置 API 路由 | ⏳ 待开始 | - |
| - | 集成自动保存 | ⏳ 待开始 | - |
| - | 修复死代码警告 | ⏳ 待开始 | - |

---

## 💡 建议

1. **立即开始实施**：第一步是后续所有工作的基础，应优先完成
2. **分批处理**：可以先实现核心功能（列表、详情、创建），再实现高级功能（对比、恢复、撤销/重做）
3. **逐步测试**：每完成一个 Handler 就进行测试，确保功能正常
4. **保留文档**：在实施过程中记录遇到的问题和解决方案

---

## 📝 总结

**第一步现状**：完全未开始（0%）

**关键问题**：
1. Handler 层文件不存在
2. API 路由未配置
3. 自动保存未集成
4. 存在大量死代码警告（211 处）

**建议行动**：立即开始实施第一步，预计 5-7 小时可完成。

**完成后效果**：
- 8 个完整的 API 端点可用
- 自动保存功能正常工作
- 移除 51 处死代码警告
- 为后续前端集成奠定基础