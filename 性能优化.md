经过对整个代码库的详细分析，我发现了以下关键性能问题：
---
性能分析报告
1. 严重 页面渲染 N+1 查询问题 (src/templates/mod.rs)
每个页面请求都会触发 30-50+ 次单独的 SQL 查询来加载设置。以 create_index_context() 为例：
- 先逐个查 8 个 template_* 设置
- 再调 load_template_settings() 又逐个查 40+ 个设置
- 再查 4 个备案信息
每个设置项都是一次独立的 SettingRepository::get() 调用。
修复建议：添加批量加载方法，一次查询所有设置：
// 在 SettingRepository 中添加
pub fn get_all_as_map(conn: &rusqlite::Connection) -> Result<HashMap<String, String>, Box<dyn std::error::Error>> {
    let mut stmt = conn.prepare("SELECT key, value FROM settings")?;
    let map: HashMap<String, String> = stmt.query_map([], |row| {
        Ok((row.get::<_, String>(0)?, row.get::<_, String>(1)?))
    })?.filter_map(|r| r.ok()).collect();
    Ok(map)
}
2. 严重 设置无缓存 (src/templates/mod.rs)
load_template_settings() 和 load_appearance_settings() 每次页面请求都查数据库。设置数据几乎不变，应该缓存。
修复建议：使用 moka 或 Arc<RwLock> 缓存设置，修改时才失效。
3. 严重 PassageRepository count 缓存无效 (src/db/repositories.rs:43-46, src/app_state.rs:69-71)
// app_state.rs:69 - 每次请求都创建新的 Repository，缓存无意义
pub fn passage_repository(&self) -> repositories::PassageRepository {
    repositories::PassageRepository::new(self.get_pool()) // 新建实例，count_cache 是空的！
}
PassageRepository 的 count_cache 和 count_published_cache 是每个实例独立的，但每次请求都 PassageRepository::new() 创建新实例，缓存完全无效。
修复建议：将 count 缓存提升到 AppState 层级使用 Arc 共享，或让 Repository 成为单例。
4. 严重 async 方法实为同步阻塞 (src/db/repositories.rs)
所有 pub async fn 方法内部没有任何 .await，全都是同步阻塞调用（self.pool.get()?, conn.prepare(), conn.execute() 等）。这会阻塞 actix-web 的工作线程。
修复建议：使用 web::block() 将数据库操作卸载到阻塞线程池：
pub async fn get_by_id(&self, id: i64) -> Result<Passage, Box<dyn std::error::Error>> {
    let pool = self.pool.clone();
    web::block(move || {
        let conn = pool.get()?;
        // ... 同步操作
    }).await.map_err(|e| Box::new(e) as Box<dyn std::error::Error>)
}
5. 严重 Release 编译优化配置错误 (Cargo.toml:132-133)
[profile.release]
opt-level = "z"   # ← 优化二进制大小，牺牲运行时性能！
lto = "fat"
opt-level = "z" 是为嵌入式/大小受限场景设计的，严重损害运行时性能。对服务端应用应该用 opt-level = 3 或至少 2。lto = "fat" 配合 opt-level = "z" 进一步加大编译时间却损失了运行时速度。
修复建议：
[profile.release]
opt-level = 3
lto = "thin"      # thin LTO 平衡编译时间和运行时性能
codegen-units = 1
6. 中等 SQLite PRAGMA 只在初始化连接上设置 (src/db/init.rs:39-98)
PRAGMAs（busy_timeout, journal_mode=WAL, cache_size 等）只在 init_db 的第一个连接上设置。r2d2 连接池创建的新连接不会继承这些设置。
修复建议：使用 SqliteConnectionManager::custom 或 r2d2_sqlite 的初始化回调：
let manager = SqliteConnectionManager::file(db_path)
    .with_init(|conn| {
        conn.execute_batch("PRAGMA journal_mode=WAL; PRAGMA busy_timeout=30000; PRAGMA synchronous=NORMAL; PRAGMA cache_size=-64000; PRAGMA temp_store=MEMORY; PRAGMA mmap_size=268435456;")?;
        Ok(())
    });
7. 中等 view_batch 批量插入使用逐行 INSERT (src/view_batch.rs:267-283)
在事务中逐条 INSERT，而非使用批量 VALUES 语句。
修复建议：构建批量 INSERT 语句：
// INSERT INTO article_views (...) VALUES (...), (...), (...)
let placeholders: Vec<String> = batch.iter().enumerate()
    .map(|(i, _)| format!("(?, ?, ?, ?, ?, ?, ?, ?, ?)"))
    .collect();
let sql = format!("INSERT INTO article_views (...) VALUES {}", placeholders.join(", "));
8. 中等 缓存管理器日志使用 eprintln! (src/cache/manager.rs)
CacheManager::get() 和 CacheManager::set() 热路径中使用 eprintln!，在高并发下导致 I/O 阻塞和锁竞争。
修复建议：替换为 tracing::debug!() 或 tracing::warn!()。
9. 低 数据库查询未使用 Prepared Statement 缓存 (src/db/repositories.rs)
每个查询都通过 conn.prepare() 创建新的 statement，对于频繁执行的查询（如 SELECT COUNT(*), 按 ID/UUID 查询等），应缓存 prepared statement。
10. 低 模板渲染无 HTTP 缓存头 (src/templates/mod.rs:566-577)
所有页面返回 Cache-Control: no-cache。对于静态页面元素，应设置适当的缓存策略减少重复请求。
---
优先修复顺序
优先级	问题
P0	Release 编译 opt-level="z"→3
P0	设置批量加载+缓存
P0	Repository count 缓存共享
P1	SQLite PRAGMA 连接池初始化
P1	async 方法使用 web::block()
P2	view_batch 批量 INSERT
P2	缓存日志改用 tracing
需要我开始修复这些问题吗？
