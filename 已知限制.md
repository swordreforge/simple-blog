# 已知限制

## 第二阶段：持久化模块限制

### RouteStorage::save() 方法限制

**问题描述**：
- `FileStorage` 的 `save()` 方法目前返回错误，因为 trait 对象无法直接访问 `SimpleRoute` 的字段
- 这导致无法将 `HashMap<String, Box<dyn RouteEntry>>` 序列化到 JSON 文件

**当前状态**：
```rust
impl RouteStorage for FileStorage {
    async fn save(&self, _routes: &HashMap<String, Box<dyn RouteEntry>>) -> Result<(), Box<dyn Error>> {
        // 暂时返回错误
        Err("RouteStorage::save for FileStorage requires full implementation in later phase. Currently only loading is supported.".into())
    }
}
```

**影响范围**：
- 无法将路由表保存到文件系统
- `load()` 方法可以正常工作（支持从 JSON 文件加载路由）

**后续解决方案**：
需要在后续阶段实现更完善的序列化机制，可能的方案包括：

1. **类型注册表**：为每种 RouteEntry 实现类型注册和反序列化
2. **类型标识符**：在序列化时存储类型信息，反序列化时根据类型信息创建实例
3. **Any trait**：使用 `std::any::Any` 进行类型转换和序列化

### MemoryStorage 的 RouteStorage 实现

**问题描述**：
- `MemoryStorage` 目前只实现了 `KeyValueStorage`，没有实现 `RouteStorage`
- 原因与 `FileStorage` 相同：无法序列化和反序列化 `RouteEntry` trait 对象

**当前状态**：
```rust
// MemoryStorage 目前只实现 KeyValueStorage，不实现 RouteStorage
// 因为 RouteStorage 需要序列化和反序列化 RouteEntry trait 对象
// 这需要更复杂的设计，将在后续阶段实现
```

**影响范围**：
- `MemoryStorage` 只能存储字符串键值对
- 无法直接用于路由持久化

**后续解决方案**：
与 `FileStorage` 相同，需要在后续阶段实现统一的序列化机制。

## 第一阶段：核心基础模块限制

### RouteEntry trait 对象限制

**问题描述**：
- trait 对象无法直接访问实现类型的私有字段
- 这使得序列化和反序列化变得复杂

**影响范围**：
- 阻碍了持久化功能的完整实现
- 限制了 RouteEntry 的扩展性

## 解决方案规划

### 阶段三/四：Actix 集成模块

在 Actix 集成阶段，可以考虑以下改进：

1. **简化序列化**：
   - 限制只支持 `SimpleRoute` 类型的路由
   - 在文档中明确说明限制

2. **增强 RouteEntry trait**：
   ```rust
   pub trait RouteEntry: Send + Sync + 'static + std::fmt::Debug {
       fn handle(&self, req: &HttpRequest) -> Pin<Box<dyn Future<Output = HttpResponse> + Send>>;

       // 新增序列化方法
       fn serialize(&self) -> Result<String, Box<dyn Error>>;
       fn deserialize(data: &str) -> Result<Box<dyn Self>, Box<dyn Error>>;
   }
   ```

3. **使用类型擦除**：
   - 实现一个注册表，将类型名称映射到工厂函数
   - 在序列化时保存类型名称和字段
   - 在反序列化时使用注册表创建实例

## 技术债务跟踪

- [ ] 实现 RouteEntry 的完整序列化支持
- [ ] 完善 FileStorage::save() 方法
- [ ] 为 MemoryStorage 添加 RouteStorage 实现
- [ ] 添加对自定义 RouteEntry 类型的持久化支持
- [ ] 考虑添加数据库存储支持（SQLite、PostgreSQL）

## 备注

这些限制不会影响第一阶段和第二阶段的核心功能：
- 路由表的增删查功能完全正常
- KeyValueStorage 接口完整可用
- 所有单元测试和文档测试通过

限制主要影响路由的持久化保存功能，这是一个高级特性，可以在后续阶段逐步完善。