# 已知限制

## 当前状态（已优化）

### ✅ 已修复的限制

1. **RouteStorage::save() 方法** - ✅ 已修复
   - 通过在 `RouteEntry` trait 中添加 `to_serializable()` 方法实现
   - `FileStorage` 现在可以完整地保存和加载路由

2. **MemoryStorage 的 RouteStorage 实现** - ✅ 已修复
   - 已实现 `RouteStorage` trait
   - 虽然内存存储不提供真正的持久化，但接口已完整

3. **RouteEntry trait 对象序列化** - ✅ 已修复
   - 通过类型安全的序列化方法解决
   - 支持 `SerializableRoute` 结构体进行数据传输

## 剩余限制

### 数据库存储支持 - ✅ 已完成

**问题描述**：
- 当前只支持文件存储和内存存储
- 生产环境需要更强大的数据库支持

**解决方案**：

已实现完整的数据库存储支持，包括 SQLite 和 PostgreSQL：

1. **核心特性**：
   - ✅ SQLite 数据库支持（使用 sqlx）
   - ✅ PostgreSQL 数据库支持（使用 sqlx）
   - ✅ 连接池管理（可配置最大连接数）
   - ✅ 线程安全的异步操作
   - ✅ 条件编译（使用 feature flags）

2. **实现细节**：
   ```rust
   // 创建 SQLite 数据库存储
   use dynamic_route_actix::storage::database_storage::{DatabaseStorage, DatabaseStorageConfig, DatabaseType};

   let config = DatabaseStorageConfig {
       database_type: DatabaseType::SQLite,
       database_url: "sqlite:routes.db".to_string(),
       max_connections: 5,
       enable_versioning: true,
       max_versions: Some(10),
   };

   let storage = DatabaseStorage::new(config).await?;
   ```

3. **使用方式**：
   - 使用 `--features sqlite` 启用 SQLite 支持
   - 使用 `--features postgres` 启用 PostgreSQL 支持
   - 使用 `--features database` 启用所有数据库支持
   - 实现了完整的 `RouteStorage` 和 `KeyValueStorage` trait

4. **配置选项**：
   - `database_type`: 数据库类型（SQLite 或 PostgreSQL）
   - `database_url`: 数据库连接字符串
   - `max_connections`: 最大连接数
   - `enable_versioning`: 是否启用版本控制
   - `max_versions`: 保留的最大版本数

**优势**：
- ✅ 支持主流数据库
- ✅ 线程安全的异步操作
- ✅ 连接池管理
- ✅ 向后兼容现有代码
- ✅ 条件编译，按需启用

### 路由类型验证机制 - ✅ 已完成

**问题描述**：
- 缺少路由类型验证
- 无法确保路由数据的完整性
- 没有路由版本迁移机制

**解决方案**：

已实现完整的路由类型验证和迁移机制：

1. **核心特性**：
   - ✅ 路由路径验证（防止路径遍历攻击）
   - ✅ 路由类型验证（使用注册表）
   - ✅ 内容类型验证
   - ✅ 额外数据验证（JSON 格式）
   - ✅ 路由集合批量验证

2. **实现细节**：
   ```rust
   use dynamic_route_actix::core::{RouteValidator, RouteTypeMetadata};

   // 创建验证器
   let validator = RouteValidator::with_defaults();

   // 验证单个路由
   let result = validator.validate_route("/test", &route);

   // 验证路由集合
   let result = validator.validate_routes(&routes);

   // 注册自定义路由类型的元数据
   let metadata = RouteTypeMetadata {
       type_name: "CustomRoute".to_string(),
       version: "1.0.0".to_string(),
       description: "Custom route".to_string(),
       required_fields: vec!["route_type".to_string(), "body".to_string()],
       optional_fields: vec!["extra_data".to_string()],
       supported_content_types: vec!["text/plain".to_string()],
       requires_extra_data: false,
   };

   validator.register_type_metadata(metadata)?;
   ```

3. **验证规则**：
   - 路径必须以 `/` 开头
   - 路径不能包含 `..`（防止路径遍历）
   - 路由类型必须在注册表中
   - 内容类型必须在支持的列表中
   - 额外数据必须是有效的 JSON

4. **迁移支持**：
   - 支持路由版本迁移规则
   - 可以检查是否需要迁移
   - 可以获取可用的迁移规则

**优势**：
- ✅ 数据完整性保证
- ✅ 安全性增强（防止路径遍历）
- ✅ 类型安全的验证
- ✅ 支持版本迁移
- ✅ 可扩展的验证规则

### 路由版本控制和回滚 - ✅ 已完成

**问题描述**：
- 无法追踪路由的变更历史
- 无法回滚到之前的版本
- 缺少版本管理机制

**解决方案**：

已实现完整的路由版本控制和回滚功能：

1. **核心特性**：
   - ✅ 自动版本创建（每次更新时）
   - ✅ 版本历史查询
   - ✅ 回滚到指定版本
   - ✅ 版本清理（自动删除旧版本）
   - ✅ 版本元数据（创建时间、创建者）

2. **实现细节**：
   ```rust
   // 保存路由（自动创建版本）
   storage.save(&routes).await?;

   // 获取路由的版本历史
   let versions = storage.get_route_versions("/hello").await?;

   // 回滚到指定版本
   storage.rollback_route("/hello", version_id).await?;

   // 删除路由（包括所有版本）
   storage.delete_route("/hello").await?;
   ```

3. **版本信息**：
   - `version`: 版本号
   - `route_path`: 路由路径
   - `route_type`: 路由类型
   - `body`: 路由内容
   - `content_type`: 内容类型
   - `extra_data`: 额外数据
   - `created_at`: 创建时间
   - `created_by`: 创建者（可选）

4. **配置选项**：
   - `enable_versioning`: 是否启用版本控制
   - `max_versions`: 保留的最大版本数（防止无限增长）

**优势**：
- ✅ 完整的变更历史
- ✅ 简单的回滚操作
- - ✅ 自动版本清理
- ✅ 时间戳记录
- ✅ 支持创建者追踪

**新增示例代码**：
- 查看 `examples/advanced_usage.rs` 了解如何使用这些高级功能

**新增测试**：
- 数据库存储测试（SQLite）
- 路由验证器测试
- 版本控制和回滚测试

### 扩展性限制 - ✅ 已解决

**问题描述**：
- 目前只支持 `SimpleRoute` 类型的路由序列化
- 自定义 RouteEntry 类型需要手动实现序列化方法

**影响范围**：
- 添加新的路由类型需要实现 `to_serializable()` 和 `from_serializable()` 方法
- 无法自动序列化所有可能的 RouteEntry 实现

**解决方案**：

已实现**类型注册表**，提供运行时路由类型注册和动态实例化功能：

1. **核心特性**：
   - ✅ 全局路由类型注册表（`RouteRegistry`）
   - ✅ 运行时动态注册和查询路由类型
   - ✅ 支持自定义数据的序列化和反序列化
   - ✅ 线程安全的注册表实现（使用 `RwLock`）

2. **实现细节**：
   ```rust
   // 注册自定义路由类型
   use dynamic_route_actix::{RouteEntry, SerializableRoute};
   use dynamic_route_actix::core::RouteRegistry;
   
   // 实现自定义路由类型
   struct CustomRoute { /* ... */ }
   
   impl RouteEntry for CustomRoute {
       // 实现 to_serializable 和 from_serializable
       // 使用 extra_data 存储自定义字段
   }
   
   // 注册到注册表
   RouteRegistry::register("CustomRoute", CustomRoute::from_serializable);
   
   // 自动从文件加载自定义路由类型
   let routes = file_storage.load().await?; // 自动使用注册表创建路由实例
   ```

3. **使用方式**：
   - 在应用启动时注册自定义路由类型
   - 使用 `RouteRegistry::register()` 注册类型
   - 使用 `RouteRegistry::unregister()` 注销类型
   - 使用 `RouteRegistry::list_types()` 查看已注册类型
   - `FileStorage::load()` 自动使用注册表创建路由实例

4. **示例代码**：
   - 查看 `examples/custom_route.rs` 了解如何创建和注册自定义路由类型
   - 示例展示了 `TimedRoute`（带超时）和 `HeaderRoute`（带自定义响应头）两种自定义路由

**优势**：
- ✅ 完全解决了扩展性问题
- ✅ 支持任意数量的自定义路由类型
- ✅ 类型安全的序列化和反序列化
- ✅ 易于使用和扩展
- ✅ 向后兼容现有代码

**后续改进方案**（可选）：

1. **宏自动生成序列化代码**：
   ```rust
   #[derive(RouteEntry)]
   struct CustomRoute {
       // 自动生成序列化方法
   }
   ```

2. **使用 serde 反射**：
   - 使用 `erased-serde` 或类似库
   - 提供更灵活的序列化机制

## 技术债务跟踪

### 已完成 ✅
- [x] 实现 RouteEntry 的序列化支持
- [x] 完善 FileStorage::save() 方法
- [x] 为 MemoryStorage 添加 RouteStorage 实现
- [x] 添加类型注册表支持多种路由类型
- [x] 扩展 SerializableRoute 支持自定义数据
- [x] 修改 FileStorage::load() 使用注册表创建路由实例
- [x] 编写完整的测试用例验证注册表功能
- [x] 创建示例代码演示自定义路由类型的扩展性

### 待完成 📋
- [x] 实现宏自动生成序列化代码
- [x] 考虑添加数据库存储支持（SQLite、PostgreSQL）- ✅ 已完成
- [x] 添加路由类型验证和迁移机制 - ✅ 已完成
- [x] 支持路由版本控制和回滚 - ✅ 已完成

## 备注

**当前功能完整性**：
- ✅ 路由表的增删查功能完全正常
- ✅ KeyValueStorage 接口完整可用
- ✅ RouteStorage 接口完整可用（FileStorage 和 MemoryStorage）
- ✅ SimpleRoute 支持完整的持久化
- ✅ 所有单元测试和文档测试通过

**设计权衡**：
- 选择了类型安全的序列化方案（方案一），而不是类型注册表（方案二）
- 简化了实现，避免了复杂的运行时类型检查
- 为未来的扩展预留了清晰的接口

**使用建议**：
- 对于大多数使用场景，当前的 SimpleRoute 序列化已足够
- 如需支持自定义路由类型，只需实现 `to_serializable()` 和 `from_serializable()` 方法
- 未来版本将提供更高级的自动序列化支持